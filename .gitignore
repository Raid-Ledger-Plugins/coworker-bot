node_modules/
dist/
.env
.env.local
*.log
.DS_Store
data/
models/

# The unsliced source download is too large to ship; only the trimmed
# per-utterance clips under clips/<category>/ are committed.
clips/_source.*
